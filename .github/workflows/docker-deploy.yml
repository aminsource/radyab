# name: Build and Deploy with Docker

# on:
#   push:
#     branches:
#       - develop # Trigger this workflow when pushing to the 'develop' branch

# jobs:
#   deploy:
#     runs-on: self-hosted # or 'ubuntu-latest' if using GitHub-hosted runners with Docker pre-installed
#     steps:
#       # Checkout the code
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # Build the Docker image
#       - name: Build tracker image
#         run: docker build -t tracker:latest .

#       # Stop the existing Docker container
#       - name: Stop existing container
#         run: docker stop tracker-umami-1 || true

#       # Remove the existing Docker container
#       - name: Remove existing container
#         run: docker rm tracker-umami-1 || true

#       # Build the Docker image
#       - name: Build Compose
#         run: docker compose up -d
